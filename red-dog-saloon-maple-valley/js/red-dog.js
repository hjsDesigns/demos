(()=>{'use strict';
 const roomData={bar:['01.jpg','Neon signs and seating inside The Red Dog Saloon','A familiar bar with plenty of character.'],garden:['27.jpg','Patrons in the Red Dog Saloon beer garden','Take your conversation outside.'],stage:['15.jpg','Covered stage and tables at The Red Dog Saloon','A covered stage for a night of live music.']};
 const tabs=[...document.querySelectorAll('[data-room]')],photo=document.querySelector('.room-photo'),panel=document.querySelector('#room-panel');
 function select(tab){tabs.forEach(t=>{const on=t===tab;t.setAttribute('aria-selected',String(on));t.tabIndex=on?0:-1});const d=roomData[tab.dataset.room];photo.src='images/'+d[0];photo.alt=d[1];document.querySelector('.room-caption').textContent=d[2];panel.setAttribute('aria-labelledby',tab.id)}
 tabs.forEach((t,i)=>{t.addEventListener('click',()=>select(t));t.addEventListener('keydown',e=>{let n;if(e.key==='ArrowRight')n=(i+1)%tabs.length;if(e.key==='ArrowLeft')n=(i+tabs.length-1)%tabs.length;if(e.key==='Home')n=0;if(e.key==='End')n=tabs.length-1;if(n!==undefined){e.preventDefault();select(tabs[n]);tabs[n].focus()}})});
 const modal=document.querySelector('.photo-dialog');document.querySelectorAll('[data-photo]').forEach(b=>b.addEventListener('click',()=>{modal.querySelector('img').src=b.dataset.photo;modal.querySelector('img').alt=b.dataset.caption;modal.querySelector('p').textContent=b.dataset.caption;modal.showModal()}));modal.querySelector('.photo-close').addEventListener('click',()=>modal.close());modal.addEventListener('click',e=>{if(e.target===modal){const r=modal.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)modal.close()}});
 document.querySelectorAll('[data-event-until]').forEach(el=>{if(Date.now()>Date.parse(el.dataset.eventUntil))el.hidden=true});
})();
